# Infrastructure

Infrastructure-as-Code and local development environment configuration.

## Structure

```
infra/
├── terraform/       # AWS infrastructure (VPC, ECS, MSK, RDS, Redis)
└── docker-compose/  # Local development environment
```
