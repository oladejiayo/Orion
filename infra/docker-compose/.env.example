# =============================================================================
# Orion Platform — Local Development Environment Variables
# =============================================================================
# Copy this file to .env in the same directory:
#   cp .env.example .env
#
# These are LOCAL DEVELOPMENT defaults only — never use in production.
# The .env file is gitignored to prevent credential leaks.
# =============================================================================

# ---------- PostgreSQL ----------
POSTGRES_USER=orion
POSTGRES_PASSWORD=orion_dev_password
POSTGRES_DB=orion

# ---------- pgAdmin ----------
PGADMIN_EMAIL=admin@orion.local
PGADMIN_PASSWORD=admin

# ---------- Redis (optional overrides) ----------
# REDIS_PASSWORD=optional_password

# ---------- Redpanda (optional overrides) ----------
# REDPANDA_BROKER_ID=0
